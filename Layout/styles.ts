import { StyleSheet, ViewStyle } from 'react-native';

export const GradientLayoutStyles = StyleSheet.create({
    container: {
        flex: 1,
        width: '100%',
        height: '100%',
    } as ViewStyle,

    overlay: {
        flex: 1,
    },
});
